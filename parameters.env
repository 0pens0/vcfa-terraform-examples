# VCF-A Terraform Deployment Parameters
# Copy this file and update the values for your environment

# =============================================================================
# REQUIRED PARAMETERS - You MUST update these values
# =============================================================================

# VCF-A Connection Details
VCFA_URL="https://your-vcfa-instance.example.com"
VCFA_REFRESH_TOKEN="your-refresh-token-here"
VCFA_ORG="your-org-name"

# =============================================================================
# OPTIONAL PARAMETERS - Update these if your environment differs from defaults
# =============================================================================

# VCF-A Environment Configuration
# Default values are provided, but you can override them for your environment
PROJECT_NAME="default-project"
REGION_NAME="west"
VPC_NAME="west-Default-VPC"
ZONE_NAME="z-wld-a"

# VM Configuration (only used for virtual-machine example)
VM_USER_PASSWORD="VMware123!"

# =============================================================================
# PARAMETER DESCRIPTIONS
# =============================================================================

# VCFA_URL: The URL of your VCF-A instance
#   Example: https://vcfa.company.com
#   Get this from your VCF-A login page

# VCFA_REFRESH_TOKEN: Your VCF-A refresh token
#   Get this from VCF-A UI: Profile -> Generate Refresh Token
#   Example: eyJhbGciOiJSUzI1NiIsInR5cCI6IkpXVCJ9...

# VCFA_ORG: Your VCF-A organization name
#   This is the organization where resources will be created
#   Example: "my-company" or "acme"

# PROJECT_NAME: VCF-A project name
#   Default: "default-project"
#   Change if you want to use a different project

# REGION_NAME: VCF-A region name
#   Default: "west"
#   Change to match your VCF-A region

# VPC_NAME: VCF-A VPC name
#   Default: "west-Default-VPC"
#   Change to match your VCF-A VPC

# ZONE_NAME: VCF-A zone name
#   Default: "z-wld-a"
#   Change to match your VCF-A zone

# VM_USER_PASSWORD: Password for VM user (virtual-machine example only)
#   Default: "VMware123!"
#   Change to a secure password for your VM 